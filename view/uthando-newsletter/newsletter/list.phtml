
<?php $this->headTitle('Newsletter') ?>

<?php if (count($this->models) > 0): ?>
    <div class="panel panel-default">
        <table class="table table-hover table-bordered table-condensed">
            <thead>
            <tr>
                <th data-field="newsletterId">Id</th>
                <th data-field="name">Name</th>
                <th data-field="description">Description</th>
                <th data-field="enabled">Enabled</th>
                <th>Actions</th>
            </tr>
            </thead>
            <tbody>
            <?php /* @var $row \UthandoNewsletter\Model\Newsletter */ ?>
            <?php foreach($this->models as $row): ?>
                <tr>
                    <td><?=$this->escapeHtml($row->getNewsletterId()); ?></td>
                    <td><?=$this->escapeHtml($row->getName()); ?></td>
                    <td><?=$this->escapeHtml($row->getDescription()); ?></td>
                    <td><?=$this->enabled($row, [
                        'table' => 'newsletter',
                        'route' => 'admin/newsletter/edit',
                    ]); ?></td>
                    <td>
                        <?=$this->partial('uthando-admin/partial/edit-actions', [
                            'pk'		=> 'newsletterId',
                            'id'		=> $row->getNewsletterId(),
                            'tableName'	=> 'newsletter',
                            'route'		=> 'admin/newsletter/edit',
                        ]);?>
                    </td>
                </tr>
            <?php endforeach; ?>
            </tbody>
        </table>
    </div>

    <?=$this->paginationControl(
        $this->models,
        'Elastic',
        'application/partial/paginate',
        ['route' => 'admin/newsletter/page']
    ); ?>

<?php else: ?>
    <h4>There are no Newsletters yet.</h4>
<?php endif; ?>